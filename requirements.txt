# V2 dependencies
# Phase 2: foundation + baseline
# Phase 3: document ingestion

pydantic>=2.0
pyyaml>=6.0
numpy>=1.24
scikit-learn>=1.3

# Document ingestion (Phase 3)
pypdf>=4.0          # maintained PDF text extraction (successor to PyPDF2)
python-docx>=1.1.0  # DOCX paragraph and table extraction

# Development / testing
pytest>=7.4
pytest-cov>=4.1