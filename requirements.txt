# V2 runtime dependencies

pydantic>=2.0
pyyaml>=6.0
numpy>=1.24
scikit-learn>=1.3

# API
fastapi>=0.100
uvicorn>=0.23
python-multipart>=0.0.6  # required for FastAPI form data endpoints
httpx>=0.24              # test client for FastAPI

# Document ingestion
pypdf>=4.0               # PDF text extraction
python-docx>=1.1.0       # DOCX paragraph and table extraction

# Semantic embeddings (optional at test time; mocked in CI)
sentence-transformers>=2.2.2

# UI
streamlit>=1.28

# Development / testing
pytest>=7.4
pytest-cov>=4.1